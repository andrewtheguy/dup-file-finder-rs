with dup as (select file.hash_id, count(file.file_path) as hash_count from file group by 1 having count(file.file_path) > 1) select file.*,file_hash.file_size,hash_count from file INNER join dup on file.hash_id = dup.hash_id inner join file_hash on file.hash_id=file_hash.id order by file_path asc